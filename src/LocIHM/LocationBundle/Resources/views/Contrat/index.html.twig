{% extends 'LocIHMLocationBundle:Admin:layout.html.twig' %}

{% block corps -%}

    <!-- Header -->
    <div class="row">
        <div class="small-12 columns">
          <h1> {{'titre.contrat'|trans}} </h1>
        </div>
    </div>

    <div class="row">
        <div class="small-12 columns">
            <table>
                <thead>
                    <tr>
                        <th>{{'word.table.id'|trans}}</th>
                        <th>{{'word.table.contrat.datedebut'|trans}}</th>
                        <th>{{'word.table.contrat.datefin'|trans}}</th>
                        <th>{{'word.table.action.title'|trans}}</th>
                    </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('contrat_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{% if entity.dateDebut %}{{ entity.dateDebut|date('Y-m-d H:i:s') }}{% endif %}</td>
                        <td>{% if entity.dateFin %}{{ entity.dateFin|date('Y-m-d H:i:s') }}{% endif %}</td>
                        <td>
                            <a href="{{ path('contrat_show', { 'id': entity.id }) }}" class="button tiny">{{'word.table.action.show'|trans}}</a>
                            <a href="{{ path('contrat_edit', { 'id': entity.id }) }}" class="button tiny">{{'word.table.action.edit'|trans}}</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="row">
        <div class="small-12 columns">
            <a href="{{ path('contrat_new') }}" class="button">
                {{'form.add.new.contrat'|trans}}
            </a>
        </div>
    </div>

    {% endblock %}
