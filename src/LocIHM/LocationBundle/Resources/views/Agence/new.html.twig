{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Agence creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('agence') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
