{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Forfait creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('forfait') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
